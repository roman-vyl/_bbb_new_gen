"""EMA pullback-oriented family (Stage 2 pipeline decomposition).

Layout: ``features/`` (calculations + profile), ``components/`` (pipeline stages
and registry), ``execution/`` (signals, trade management, JSON artifacts); root
holds ``config``, ``variants``, ``run``, and ``instance``.
"""
